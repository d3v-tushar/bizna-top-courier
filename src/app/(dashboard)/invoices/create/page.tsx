export default async function CreateInvoicePage({
  searchParams,
}: {
  searchParams: { [key: string]: string | undefined };
}) {
  // const { clientQuery } = searchParams;
  // const [clientList, cargoList, zoneList] = await Promise.all([
  //   getClientUsers(clientQuery),
  //   getCargoList(),
  //   getZoneWithOptions(),
  // ]); // Wait for all promises to resolve before rendering the page

  // console.log('server client list', clientQuery, clientList);

  return <>Under Const</>;
}
